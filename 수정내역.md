# 음악극 자료 수정 내역

이 문서는 `PPT1_도입과서양음악극.html`, `PPT2_한국음악극.html` 등 슬라이드 자료를 다듬으며 진행한 수정 사항을 정리한 기록입니다.

## 1. 전체 슬라이드 공통 (8개 파일 모두 적용)
대상: `PPT1_도입과서양음악극.html`, `PPT2_한국음악극.html`, `PPT3_미니북활동.html`, `01_opera.html`, `02_musical.html`, `03_pansori.html`, `04_changgeuk.html`, `음악극_드림스타일.html`

- **위/아래 여백 통일**: 제목(`.stitle`)이 있는 슬라이드는 모두 제목 바로 아래 같은 위치에서 내용이 시작하도록 변경 (`justify-content:flex-start`). 표지처럼 제목이 없는 슬라이드는 기존처럼 중앙 정렬 유지.
- **화면 자동 맞춤 기능 추가**: 슬라이드를 넘길 때마다 내용 크기와 화면 크기를 비교해서, 내용이 화면보다 크면 자동으로 비율을 줄여 한 화면 안에 다 들어오도록 하는 기능(`fitSlide`)을 추가. 창 크기가 바뀌어도 다시 계산됨. (내용이 작을 땐 억지로 확대하지 않음)

## 2. PPT1 — 도입과 서양 음악극

### 앤드루 로이드 웨버 슬라이드
- 마제스틱 극장 사진 → 실제 인물 사진(`images/앤드루 로이드 웨버.jpg`)으로 교체
- 사진에 맞지 않던 카드 설명("사진 속 브로드웨이 공연장") → "브로드웨이 장기 공연장"으로 수정
- 사진을 왼쪽 끝, 정보 카드 2개를 오른쪽 끝으로 배치(여백 활용), 사진 확대
- 이름 텍스트("앤드루 로이드 웨버") 삭제 — 제목에 이미 이름이 있어 중복이었음
- 본문에 "캣츠", "에비타", "30년"을 클릭하면 나타나는 빈칸으로 추가

### 모차르트 슬라이드
- 좌우 분할(이미지·텍스트) 레이아웃 → 이미지 위/이름 아래 전체 폭 배치로 변경
- "볼프강 아마데우스 모차르트"가 "모"/"차르트"로 잘못 줄바꿈되던 문제 수정 (`word-break:keep-all` 추가, 줄바꿈 위치 명시)
- 정보 카드 3개를 사진 좌우로 분산 배치 → 이후 사진 왼쪽, 카드 전부 오른쪽으로 재배치(여백 활용)
- 이름 텍스트 삭제, 본문 글자 확대
- "두 달"을 클릭하면 나타나는 빈칸으로 추가

### 오페라의 특징 / 뮤지컬의 특징
- 두 슬라이드 레이아웃을 동일하게 통일: 무대 그림을 왼쪽에 크게, 특징 카드 3개를 오른쪽에 세로로 배치
- 카드 속 핵심 단어(마이크 없이·오케스트라 반주·벨칸토 창법 / 노래·춤·연기)를 클릭 시 나타나는 빈칸으로 변경
- "관현악단" 카드 → "오케스트라"로 명칭 변경 (본문 속 "관현악단"은 유지)

### 유명한 오페라 / 유명한 뮤지컬 목록
- 제목 글자가 기본적으로 투명 처리되어 있던 기존 버그(빈칸 클릭용 스타일을 잘못 재사용) 발견 및 수정
- 각 항목을 한 줄로 정리, 카드 폭 확대, 줄 간격 통일, 글자 확대
- 설명글이 제목 길이와 관계없이 항상 같은 위치에서 시작하도록 열 정렬
- 클릭하면 색깔 글자가 나타나는 방식으로 재변경 (밑줄 점선 → 실선 통일)
- 오페라 목록(제목+작곡가)은 왼쪽 정렬, 뮤지컬 목록(제목만)은 가운데 정렬로 구분 적용

### 오페라의 유령 인물 구성도
- 극장 사진 + 미니 카드 6개 → 마술피리 슬라이드와 동일하게 준비되어 있던 인물 관계도 이미지(캐릭터 7명 전원 포함)로 교체

### 음악극이란? (도입부)
- 하단 카드 3개의 "오페라", "뮤지컬", "판소리", "창극"을 클릭하면 나타나는 빈칸으로 변경
- "(다음 시간)" 문구 삭제

### 오페라 vs 뮤지컬 비교표
- 정답 공개 색상을 주황/금색(#ffa94d) → 청록색(#4fd8e0)으로 변경 (전역 변수라 다른 빈칸 정답 표시에도 동일 적용됨)

### 영상 링크
- "The Music of the Night" 영상 ID가 존재하지 않는 링크였던 것을 확인, 정상 작동하는 공식 영상으로 교체
- 이후 화질이 낮다는 피드백으로 Royal Albert Hall 25주년 공연 실황(고화질)으로 재교체

## 3. PPT2 — 한국 음악극

- PPT1과 동일하게 "유명한 판소리"(춘향가·심청가·흥보가·수궁가·적벽가), "유명한 창극"(춘향전·심청전·흥보전·수궁가) 목록에 열 정렬 + 클릭 시 색깔 글자 표시 적용

## 4. 디자인 시스템 정리

- 클릭하면 나타나는 빈칸 스타일(`.kw`)의 밑줄을 점선 → 실선으로 통일 (다른 빈칸 스타일과 일관성 확보)

## 5. 이번 세션 수정 내역 (2026-08-28 ~ 08-29)

### 콘텐츠 추가
- **창극의 특징**(PPT2): 도창(都唱)·작창·창작·재창조 3개 카드 추가 (기존 3개 → 6개)
- **오페라의 특징 / 뮤지컬의 특징**(PPT1): 각 3개 카드 추가 (아리아·레치타티보, 리브레토, 웅장한 무대미술 / 마이크·앰프, 화려한 무대 장치, 대중성)
- **판소리 vs 창극 — 무대 비교** 새 슬라이드 추가(PPT2): 비교 사진을 화면 가득 크게, "차이점 비교" 표 바로 다음에 배치
- **오페라 공연 그림 / 뮤지컬 공연 그림** 새 슬라이드 추가(PPT1): 각 "~의 특징" 슬라이드 다음에 화면 전체 크기로 배치 (기존 특징 슬라이드 안에 있던 작은 썸네일은 중복이라 삭제)
- **미니북 내용 구성도**(PPT3): 이미지 대신 실제 8분할 색상 배치를 그대로 재현한 HTML 카드로 제작 (위쪽 4칸은 실제 접지 인쇄물처럼 180도 회전 표시), 텍스트라 자유 수정 가능
- **미니북 만들기 순서**(PPT3): 접는 순서 6단계 실사 이미지로 "B4 접는 방법" 슬라이드 보강
- 김준수 프로필 사진 추가 (더뮤지컬 인물DB, 생년월일·소속 확인 후 반영), 이자람 프로필 사진 추가(위키백과)
- 국립창극단 소개 슬라이드의 깨진 위키미디어 이미지(잘못된 썸네일 해시 경로) 발견 및 정상 이미지로 교체

### 콘텐츠 정확성 수정
- 춘향가 감상 포인트 영상이 실제로는 "수궁가" 영상이었던 것을 발견 → 소리꾼 김준수의 「사랑가」 영상으로 교체
- 춘향전(창극) 감상 포인트 영상이 실제로는 「리어」 공연이었던 것을 발견 → 「춘향」 명장면 하이라이트로 교체 (도입부의 「춘향」 전막 영상과 겹치지 않도록 다른 클립 사용)
- 활동 안내(PPT3): "네 가지 음악극을 모두 정리" → "1개를 골라 정리"로 수정 (미니북 실제 구성과 일치하도록), 마무리 문구도 함께 수정

### 버그 수정
- **슬라이드 전체가 안 보이던 문제**: 김준수 슬라이드 편집 중 `</div>` 태그 하나가 누락되어 이후 모든 슬라이드가 그 안에 중첩되는 바람에 화면에 안 나타나던 심각한 버그 발견·수정 (전체 슬라이드 자동 순회 검증 로직으로 재발 방지 확인)
- **PPT3 클릭 반응 없음**: `.kw` 빈칸 클릭 시 실행되는 `tog()` 함수 자체가 PPT3에는 정의돼 있지 않던 것을 발견, PPT1/2에서 가져와 추가
- **한글 줄바꿈 버그**: "소리를"처럼 조사가 붙은 단어가 글자 단위로 잘려 다음 줄로 넘어가던 문제를 `word-break:keep-all`을 전역(`body`)에 적용해 근본 해결 (PPT1·2·3 전체)
- 글자를 키우는 과정에서 발생한 여러 잘림/줄바꿈/간격 문제 다수 수정 (빈칸과 "—" 기호 간격, 카드 너비 고정값 → 화면 비율 기준으로 전환 등)

### 구조 변경
- 목차 슬라이드 삭제 (PPT1·PPT2 모두) — 슬라이드 번호 자동 재정렬
- PPT3에서 "미니북 만들기 STEP 1/2", "미니북 완성 예시" 슬라이드 3개 삭제 (8장 → 5장)
- 판소리 vs 창극 무대 비교 사진 슬라이드 순서 조정 (차이점 비교 표 바로 다음으로)

### 레이아웃 · 가독성 개선
- "유명한 오페라/뮤지컬/판소리/창극과 내용 요약" 4개 슬라이드: 글자 확대, 화면 비율 기준 너비로 변경, 설명 속 핵심 키워드에 금색 강조 추가
- "◯◯ 줄거리" 계열 슬라이드(마술피리·오페라의 유령·춘향가·춘향전) 및 "학습목표", "퀴즈로 마무리": 카드를 화면 96% 너비로 확대, 본문 글자 확대, 핵심 키워드 금색 강조
- "◯◯ 감상 포인트" 계열 슬라이드(마술피리·오페라의 유령·춘향가·춘향전): 세로 배치(설명 위 + 영상 아래) → 가로 배치(영상 왼쪽 + 설명 오른쪽 큰 글자)로 재구성, 설명 속 핵심 구절에 하늘색 강조 추가
- 인물 소개 슬라이드(이자람·김준수·국립창극단·모차르트·앤드루 로이드 웨버): 사진·본문·정보카드를 한 줄 레이아웃으로 재구성해 화면 자동 축소(여백) 현상 해소

### 기타
- 표지(PPT3) 부제 문구 삭제, 상단 페이지 번호 잔여 표시("1/8") 수정

## 6. 활동지 여백 수정 및 감상문 섹션 재구성 (2026-08-29)

대상: `활동지/오페라_마술피리_활동지·정답지.html`, `활동지/뮤지컬_오페라의유령_활동지·정답지.html`, `활동지/판소리_춘향가_활동지·정답지.html`, `활동지/창극_춘향전_활동지·정답지.html` (총 8개 파일, 32페이지)

### 참고 출처 — 대취타 프로젝트의 원래 기록

이 폴더의 활동지들과 똑같은 CSS 클래스 구조를 그대로 재사용해 만들어진 `E:\2026 음악\대취타\대취타_활동지.html`/`대취타_정답지.html`에는 이미 여백 문제를 해결한 선례가 있다. `대취타/수정내역.md`에 기록된 원문은 다음과 같다:

> - A4(210×297mm) 페이지 여백 조정: 전체 폰트 약 25% 확대 + 카드 패딩·줄 간격 확대 → 이 과정에서 공통 CSS 클래스(`.divider`, `.sec-title`, `.elem-grid`)를 건드려 3페이지뿐 아니라 1·4페이지까지 함께 넘치는 문제가 발생
> - Playwright(Chromium)로 각 페이지의 실제 렌더링 높이를 직접 측정해 원인 파악 → 공통 클래스는 원래 크기로 되돌리고, 3페이지에만 인라인 스타일로 여백을 추가, 1·4페이지는 카드 패딩과 서술형 답안 줄 수를 줄여 재조정 → **8개 페이지(활동지 4장 + 정답지 4장) 전부 297.1mm로 정확히 A4에 맞춘 것을 측정으로 확인**

이번 세션에서는 위와 동일한 문제(활동지가 대취타보다 폰트가 작은 "수정 전" 버전이라 페이지 하단에 콘텐츠와 footer 사이 불필요한 빈 여백이 크게 남는 문제)를 이 8개 파일에도 동일한 방식(폰트·패딩 스케일업 + Playwright 실측 검증)으로 적용했다.

### 여백 제거 (전체 폰트/패딩 스케일업)
- 대취타에서 검증된 CSS 값(`.sec-title` 14→17.5pt, `.def-box` 12→15pt, `.char-card` 패딩 확대 등)을 그대로 가져오고, 대취타에 없는 클래스(`.story-card`·`.scene-blank`·`.ord-table` 등 "줄거리" 페이지 전용)는 같은 비율로 비례 확대
- PAGE 1(오페라·뮤지컬 개념/비교, 특히 판소리·창극은 "~란 무엇인가요" 3칸 그리드가 하나 더 있어 콘텐츠가 가장 빽빽함)에서 스케일업 직후 최대 68mm 오버플로우가 발생 → `.c-ans`에 폰트 크기가 지정되어 있지 않아 브라우저 기본값(16px)으로 렌더링되던 버그를 발견·수정하는 등, 비교표·특징 카드·보기 칩 목록의 패딩/폰트를 단계적으로 축소하며 재조정
- PAGE 2(주요 인물 카드)·PAGE 3(줄거리 순서 배열 표)·PAGE 1의 "② 비교" 섹션은 `flex:1`(+PAGE 2는 `align-content:stretch`, PAGE 1·3은 `justify-content:center` 래퍼)로 감싸 남는 세로 공간을 콘텐츠 자체가 자동으로 채우도록 구조 변경 — 폰트 크기를 페이지마다 정밀하게 맞추는 대신, 콘텐츠가 늘어나거나 카드가 넓어져 어떤 파일이든 빈 여백 없이 A4를 채우게 됨
- `.page` 상하 padding을 8mm→5mm로 축소(좌우 11mm는 인쇄 여백 확보를 위해 유지), `.p1-header` 패딩 확대와 `.p1-sub`/`.p1-info` 폰트 축소로 표지 상단부가 얇아 보이지 않으면서도 부제/메타 정보가 어색하게 줄바꿈되지 않도록 조정
- Playwright(Chromium)로 8개 파일 × 4페이지 = 32페이지 전부 실제 렌더링 높이를 측정 — 최종적으로 모든 페이지가 오버플로우 없이, footer 바로 위까지 여백 없이(gap 0.0mm) 채워지는 것을 확인

### ⑥ 감상문 쓰기 → 감상문(느낀점) 작성하기 섹션 재구성
- 기존 Q1/Q2 두 개의 구체적 질문 + 5줄 쓰기칸 구조를 삭제하고, 질문 문구 없이 자유롭게 쓸 수 있는 4줄짜리 쓰기칸 하나로 단순화
- 그 아래에 미니북 만들기 활동(PPT3)과 연계되는 네모 빈칸 2개를 신설: "미니북 표지 꾸미기", "인상 깊은 장면 그리기" — 실제 그림을 그리는 칸이 아니라, 나중에 미니북을 만들 때 표지와 인상 깊은 장면을 어떻게 표현할지 미리 스케치해보는 계획용 칸
- "⑤ 감상 포인트"의 각 POINT 카드에서 "Q. 느낀 점을 써 보세요." 문구와 그 아래 줄글 쓰기칸을 삭제(카드에는 배지·빈칸·설명만 남김) — 이 변경으로 생긴 여유 공간은 감상문 쓰기칸이 아니라 두 네모칸(`.sketch-grid`, `flex:1`)이 자동으로 흡수하도록 해서, 실제로 그 두 칸이 페이지마다 넉넉한 크기로 커지도록 함
- 8개 파일 모두 동일하게 적용 (정답지도 이 섹션은 원래 정답이 채워지지 않는 자유 서술형이라 활동지와 동일 구조로 통일)

### 이모티콘 전부 삭제
- elem-card/number-card 안의 장식용 이모티콘(🎤🎻🎭📖 등), comp-head-cell·page-footer의 장르 아이콘(🎼🥁 등), "📌 보기 :"의 핀 이모티콘, sketch-label의 📕/🎨를 전부 삭제
- 아이콘을 담고 있던 `<span class="elem-icon">`/`<div class="num-icon">` 요소 자체를 통째로 제거해 대취타 선례("이모티콘 삭제 후 남은 빈 테두리 상자도 함께 정리")와 동일하게 빈 상자가 남지 않도록 처리

### 버그 수정
- 정규식으로 num-q/write-lines 블록을 일괄 삭제하는 과정에서 non-greedy 패턴이 중첩된 `<div class="wline">` 태그에 걸려 `</div>` 하나가 부족하게 삭제되는 사고가 6개 파일에서 발생(각 페이지 구조가 어긋나는 심각한 버그) → 원인을 정확히 특정해 정규식을 명시적 반복 패턴으로 교체하고, 이미 깨진 6개 파일은 남은 잔재(wline 3개 + 고아 `</div>`)를 제거하는 방식으로 복구, 이후 전체 파일 `<div>`/`</div>` 개수 일치 여부를 스크립트로 재검증
- 편집 전 8개 원본 파일을 `활동지/_backup_20260829/`에 백업해둠

### 추가 수정 — "가운데 정렬" 방식의 시각적 여백 재발견·수정
- 위 작업에서 PAGE 1(② 비교)·PAGE 3(줄거리 순서 배열)에 쓴 `justify-content:center` 래퍼 방식은, footer까지의 DOM상 거리는 0mm로 측정되지만 실제로는 표/카드 블록이 남는 공간 안에서 가운데 정렬되면서 표 아래쪽에 하나의 큰 빈 띠가 그대로 남아 보이는 문제가 있었음(스크린샷으로 재확인 후 발견)
- `justify-content:center` 래퍼를 걷어내고, 대신 콘텐츠 자체(표의 행)가 늘어나 남는 공간을 채우도록 재수정:
  - PAGE 1 `.comp-wrap`(오페라 vs 뮤지컬 비교표): `comp-wrap`을 `flex:1;display:flex;flex-direction:column`으로, `comp-head`는 `flex-shrink:0`으로 고정, 각 `comp-row`에 `flex:1`을 줘서 비교표의 각 행 높이가 늘어나 남는 공간을 채우도록 함(부수 효과로 답 쓰는 칸도 더 넉넉해짐)
  - PAGE 3 `ord-table`(줄거리 순서 배열): `<table>` 자체에 `flex:1`을 줘서, CSS 표준 표 행 높이 분배 동작(Chromium에서 정상 동작 확인)으로 각 행이 자동으로 늘어나 남는 공간을 채우도록 함
  - 8개 파일 16곳(파일당 2곳) 전부 동일하게 수정, `<div>`/`</div>` 개수 재검증 및 32페이지 스크린샷으로 빈 띠가 남지 않는지 전부 재확인 완료

### 감상문 쓰기칸 2줄 추가 (미니북 네모칸에서 흡수)
- "⑥ 감상문(느낀점) 작성하기"의 `.wl` 줄을 4줄 → 6줄로 늘림
- 아래 "미니북 표지 꾸미기"/"인상 깊은 장면 그리기" 네모칸(`.sketch-grid`, `flex:1`)이 그만큼 자동으로 줄어들어 균형을 맞춤(별도 높이 계산 없이 flex 구조 덕분에 자동 반영)
- 8개 파일 전부 적용, `<div>` 개수 재검증 및 렌더링 확인 완료

### 비교표 정답 글자 진하게 통일
- 정답지의 "② 비교" 표(`.c-ans`) 안 빨간 정답 글자가 다른 빈칸 정답(진하게 표시됨)과 달리 얇게 보이던 문제 발견 → `.c-ans`에 `font-weight:800` 추가로 통일 (대취타 활동지에서 있었던 동일 문제의 재발)
- 8개 파일 전부 적용

### 뮤지컬 「오페라의 유령」 정답지 — 줄거리 순서 배열 정답 누락 수정
- 다른 3개 정답지와 달리 "줄거리 순서 배열" 표의 빨간 정답 번호가 비어 있던 것을 발견(원본 파일의 기존 누락) → 줄거리 순서(① 유령의 지도와 스타 탄생 → ② 크리스틴과 라울의 사랑 → ③ 유령의 질투와 혼란 → ④ 유령의 놓아줌)에 맞춰 표의 4개 행에 ④·①·③·② 순서로 빨간 정답 번호를 채워 넣음

## 7. index.html 신규 제작

- `E:\2026 음악\대취타\index.html`, `E:\2026 음악\종묘제례악 최종\종묘제례악 수업용\index.html`을 참고해 이 폴더에도 `index.html`을 새로 제작
- 디자인(남색 그라데이션 배경, 금색 큰 PPT 버튼, 작은 활동지/정답지 버튼)은 그대로 가져오되, 이 폴더는 PPT가 3개(PPT1 도입·서양 음악극 / PPT2 한국 음악극 / PPT3 미니북 활동)이고 장르별 활동지·정답지가 4쌍(오페라·뮤지컬·판소리·창극)이라 구조를 확장: 상단에 PPT 3개 버튼, 하단에 장르별 카드 4개(각 카드 안에 활동지·정답지 버튼) 배치
- 모든 링크 파일 실존 여부 확인 및 렌더링 스크린샷으로 확인 완료
- 이후 사용자 요청으로 하단 footer("경남교육청 · 음악 수업 자료 · 2026") 삭제
